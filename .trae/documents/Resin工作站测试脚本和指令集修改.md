### 1. 测试脚本调试模式修改

**目标**：在测试脚本中添加命令行输入判断，控制测试流程

**修改文件**：`test_resin_workstation_direct.py`

**修改内容**：
- 在第84-100行的测试步骤之间添加命令行输入判断
- 实现YES/NO确认机制，只有输入YES才继续执行后续测试
- 保持原有的调试模式功能不变

**具体修改点**：
1. 在反应器操作测试部分（第84行）添加输入判断
2. 实现输入验证，确保只接受YES或NO
3. 若输入NO则跳过后续测试步骤
4. 保持测试结果的正确统计

### 2. 指令集格式修改

**目标**：将指令集从JSON字符串形式改为函数语句形式

**修改文件**：`resin_workstation.py`

**修改内容**：
- 修改 `UDPClient` 类的 `send_command` 方法
- 将JSON格式命令改为 `S REACTOR_SOLUTION_ADD(1,4.2,reactor_1)` 形式
- 确保所有命令都能正确转换

**具体修改点**：
1. 在 `send_command` 方法中（第189行）修改命令构建逻辑
2. 将JSON格式转换为函数调用格式
3. 处理不同命令的参数格式
4. 确保响应解析逻辑保持兼容

### 3. 验证和测试

**验证步骤**：
1. 运行修改后的测试脚本，验证命令行输入功能
2. 检查调试模式下的输出是否正确
3. 确保所有命令都能正确转换为新格式
4. 验证设备通信功能正常

### 4. 修改注意事项

**注意事项**：
1. 保持向后兼容性，确保现有功能不受影响
2. 确保新的命令格式符合设备要求
3. 添加适当的日志记录，便于调试
4. 保持代码的可读性和可维护性

### 5. 预期效果

**预期效果**：
1. 测试脚本支持交互式调试，可控制测试流程
2. 指令集格式从JSON改为函数语句形式
3. 所有测试功能正常工作
4. 代码结构清晰，易于维护